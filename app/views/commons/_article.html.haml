= link_to article_path(article) do
  .card
    - if article.eyecatch.attached?  #アイキャッチがアップロードされていたら
      .card_image
        = image_tag article.eyecatch
    .card_content
      .card_title
        = article.title
      .card_heart
        = image_tag 'heart.svg'
        %span= article.like_count  #いいねのか数を表示
      .card_detail
        = image_tag article.user.avatar_image
        %div
          -# -if article.user.present?
          %p= article.author_name
          %p= article.display_created_at
          / article.rbのメソッドを実行 時間の表示
